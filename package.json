{
  "name": "spectrum-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing Spectrum app data over the Model Context Protocol, authorized per user",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^7.0.0",
    "vitest": "^5.0.0",
    "wrangler": "^4.28.0"
  },
  "packageManager": "pnpm@11.25.0"
}
